---
phase: 01-topicextractor-retry-bug-fix
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified: []
autonomous: true
requirements:
  - TEST-03

must_haves:
  truths:
    - "All tests across the entire test suite pass"
    - "Test coverage is 100%"
    - "ruff reports no errors or warnings"
  artifacts:
    - path: "components/topic_extractor.py"
      provides: "Ruff-clean implementation with docstring and type annotations on _parse_response"
      contains: "def _parse_response"
  key_links:
    - from: "pyproject.toml"
      to: "uv run pytest --cov=."
      via: "pytest configuration and coverage settings"
      pattern: "\\[tool\\.pytest\\.ini_options\\]"
---

<objective>
Gate the phase: verify the full test suite is green, test coverage is 100%, and
ruff reports no errors. This plan produces no file changes — it is a quality gate.

Purpose: TEST-03 — ensures the BUG-01 fix does not regress any existing tests and
meets the project's mandatory quality standards (100% coverage, ruff clean).

Output: Confirmation that the phase is complete and ready for /gsd:verify-work.
</objective>

<execution_context>
@/home/oton/projects/podcast-ad-cutter/.claude/get-shit-done/workflows/execute-plan.md
@/home/oton/projects/podcast-ad-cutter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-topicextractor-retry-bug-fix/01-CONTEXT.md
@.planning/phases/01-topicextractor-retry-bug-fix/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite with coverage and ruff lint gate</name>
  <files></files>

  <read_first>
    - components/topic_extractor.py — read the completed implementation to verify _parse_response
      has a one-line docstring and full type annotations before running ruff, as missing either
      will cause ANN/D ruff violations (pyproject.toml selects ALL rules; only D104, D107, D203,
      D213 are excluded; ANN rules are excluded only for tests/**)
    - pyproject.toml — confirm ruff rule configuration and coverage settings if any failures occur
  </read_first>

  <action>
    Run the three mandatory quality gates in sequence:

    1. Full test suite with coverage:
       `uv run pytest --cov=.`
       Expected: all tests pass, coverage 100%.

    2. Ruff lint check:
       `uv run ruff`
       Expected: no output (exit 0 = clean).

    If ruff reports errors in components/topic_extractor.py:
    - ANN errors on _parse_response: add missing type annotations
      Required signature: `def _parse_response(self, content: str, guid: str, podcast: str, title: str) -> TopicExtraction:`
    - D errors on _parse_response: add a one-line docstring as the first line of the method body:
      `"""Parse an LLM JSON response into a TopicExtraction; raises JSONDecodeError or KeyError on failure."""`
    - Any % operator in log calls: replace with f-string equivalent

    If coverage is below 100%:
    - Run `uv run pytest --cov=. --cov-report=term-missing` to identify uncovered lines
    - The retry loop branches (e.g. API failure on retry, exhausted retries fallthrough) must be
      covered; if missing, add targeted unit tests to tests/test_topic_extractor.py following
      the existing mock pattern using AsyncMock side_effect

    Do not modify any files beyond components/topic_extractor.py and (if needed)
    tests/test_topic_extractor.py.
  </action>

  <verify>
    <automated>cd /home/oton/projects/podcast-ad-cutter && uv run pytest --cov=. && uv run ruff</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest --cov=.` exits 0 with no failed tests
    - `uv run pytest --cov=.` output contains `TOTAL` line showing `100%` coverage
    - `uv run ruff` exits 0 with no output (zero errors, zero warnings)
    - components/topic_extractor.py `_parse_response` method has a docstring (grep: `"""Parse an LLM JSON`)
    - components/topic_extractor.py `_parse_response` has return type annotation (grep: `-> TopicExtraction:`)
  </acceptance_criteria>

  <done>
    `uv run pytest --cov=.` exits 0 with 100% coverage.
    `uv run ruff` exits 0 with no output.
    Phase 1 quality gate passed — ready for /gsd:verify-work.
  </done>
</task>

</tasks>

<verification>
Both commands must exit 0:
- `uv run pytest --cov=.`
- `uv run ruff`
</verification>

<success_criteria>
1. All tests in the full suite pass (no regressions from any phase)
2. Coverage report shows TOTAL 100%
3. ruff exits 0 with no errors or warnings
4. Phase 1 is complete and ready for /gsd:verify-work
</success_criteria>

<output>
After completion, create `.planning/phases/01-topicextractor-retry-bug-fix/01-02-SUMMARY.md`
</output>
