---
phase: 02-pipeline-integration-ad-detection-audio-editing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
autonomous: true
requirements:
  - EDIT-02
  - PIPE-05
must_haves:
  truths:
    - "REQUIREMENTS.md EDIT-02 states AudioEditor keeps return-None behavior (not always-produces-output)"
    - "ROADMAP.md Phase 2 success criterion 2 no longer says 'always produces an output file'"
    - "ROADMAP.md Phase 2 success criterion reflects: when no qualifying ads, original URL is preserved"
    - "PROJECT.md Active requirements list no longer contains 'AudioEditor always produces an output file'"
    - "PROJECT.md Context paragraph no longer describes always-produces-output as the intended behavior"
    - "PROJECT.md Key Decisions table reflects D-01: return-None behavior is correct, no update needed"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated EDIT-02 requirement text"
      contains: "return None"
    - path: ".planning/ROADMAP.md"
      provides: "Updated Phase 2 success criteria"
    - path: ".planning/PROJECT.md"
      provides: "Updated to remove stale always-produces-output statements"
  key_links:
    - from: ".planning/REQUIREMENTS.md EDIT-02"
      to: "CONTEXT.md D-01"
      via: "Requirement text updated to match locked decision"
      pattern: "return None"
    - from: ".planning/PROJECT.md"
      to: "CONTEXT.md D-01"
      via: "Three stale statements corrected to reflect return-None behavior"
      pattern: "return None"
---

<objective>
Update planning documents to reflect the locked decision D-01/D-02/D-03 from CONTEXT.md: AudioEditor keeps its current `return None` behavior when no qualifying ad segments exist — it does NOT always produce output.

Purpose: The ROADMAP was written before discuss-phase locked D-01. REQUIREMENTS.md EDIT-02 and ROADMAP Phase 2 success criterion 2 still describe the overridden behavior. PROJECT.md has three stale statements (lines 34, 56, 70) that also contradict D-01 — executors of plans 02-02 and 02-03 read PROJECT.md as a canonical reference. These must all be corrected before implementation begins.

Output: REQUIREMENTS.md, ROADMAP.md, and PROJECT.md updated to reflect D-01.
</objective>

<execution_context>
@/home/oton/projects/podcast-ad-cutter/.claude/get-shit-done/workflows/execute-plan.md
@/home/oton/projects/podcast-ad-cutter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-pipeline-integration-ad-detection-audio-editing/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update REQUIREMENTS.md EDIT-02 to reflect D-01</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (read current EDIT-02 text before modifying)
    - .planning/phases/02-pipeline-integration-ad-detection-audio-editing/02-CONTEXT.md (D-01, D-02, D-03 locked decisions)
  </read_first>
  <action>
    In .planning/REQUIREMENTS.md, replace the EDIT-02 requirement text.

    Current text (to find and replace):
    ```
    - [ ] **EDIT-02**: AudioEditor.edit() always produces an output file — when no qualifying ad segments exist, re-encode the input without cuts (do not return None)
    ```

    New text (exact replacement):
    ```
    - [ ] **EDIT-02**: AudioEditor.edit() keeps its current `return None` behavior when no qualifying ad segments exist. The pipeline preserves the original episode URL unchanged — no local output file is produced for clean episodes. (Decision D-01 from discuss-phase overrides the original always-produces-output requirement.)
    ```

    Also update the Traceability table entry for EDIT-02 — it is already "Pending" status, no change needed there.

    No other changes to REQUIREMENTS.md.
  </action>
  <verify>
    <automated>grep -n "return None" /home/oton/projects/podcast-ad-cutter/.planning/REQUIREMENTS.md</automated>
  </verify>
  <done>REQUIREMENTS.md EDIT-02 entry contains "return None" and no longer says "always produces an output file" or "do not return None".</done>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP.md Phase 2 success criteria to reflect D-01</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (read current Phase 2 success criteria section before modifying)
    - .planning/phases/02-pipeline-integration-ad-detection-audio-editing/02-CONTEXT.md (D-01, D-06 locked decisions)
  </read_first>
  <action>
    In .planning/ROADMAP.md, find the Phase 2 success criteria block and replace criterion 2.

    Current criterion 2 (to find and replace):
    ```
      2. AudioEditor always produces an output file — when no qualifying ad segments exist, the episode is re-encoded without cuts rather than returning None
    ```

    New criterion 2 (exact replacement):
    ```
      2. AudioEditor keeps its current return-None behavior — when no qualifying ad segments exist, the pipeline preserves the original episode URL unchanged and produces no local output file for that episode
    ```

    No other changes to ROADMAP.md.
  </action>
  <verify>
    <automated>grep -n "original episode URL" /home/oton/projects/podcast-ad-cutter/.planning/ROADMAP.md</automated>
  </verify>
  <done>ROADMAP.md Phase 2 success criterion 2 contains "original episode URL" and no longer says "always produces an output file" or "re-encoded without cuts".</done>
</task>

<task type="auto">
  <name>Task 3: Update PROJECT.md to remove three stale always-produces-output statements (per D-03)</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (read lines 30-75 before modifying — three stale entries)
    - .planning/phases/02-pipeline-integration-ad-detection-audio-editing/02-CONTEXT.md (D-01, D-03)
  </read_first>
  <action>
    Make three targeted changes to .planning/PROJECT.md. Read the file first to confirm line numbers, then apply each change:

    **Change 1 — Active requirements list (near line 34):**
    Find and remove this line entirely:
    ```
    - [ ] AudioEditor always produces an output file (even when no qualifying ads — re-encode without cuts)
    ```
    Replace with:
    ```
    - [ ] AudioEditor keeps its return-None behavior when no qualifying ad segments exist — pipeline preserves original episode URL unchanged (per D-01)
    ```

    **Change 2 — Context paragraph (near line 56):**
    Find the "No-qualifying-ads behaviour" sub-bullet in the Context section:
    ```
    - **No-qualifying-ads behaviour**: When `AudioEditor.edit()` finds no qualifying segments, it currently returns `None`. Per the project's design, a final output file must always be produced (re-encode without cuts). AudioEditor should be updated to always produce output.
    ```
    Replace with:
    ```
    - **No-qualifying-ads behaviour**: When `AudioEditor.edit()` finds no qualifying segments, it returns `None`. This is correct per D-01 — the pipeline preserves the original episode URL unchanged and produces no local file for clean episodes. AudioEditor requires no changes.
    ```

    **Change 3 — Key Decisions table (near line 70):**
    Find the first row of the Key Decisions table:
    ```
    | AudioEditor always produces output (even with no qualifying ads) | User expects a final file and RSS entry for every episode; None return would leave original feed URL unchanged | — Pending |
    ```
    Replace with:
    ```
    | AudioEditor keeps return-None behavior when no qualifying ads exist | Original URL preserved for clean episodes; no re-encoding needed; consistent with D-01 locked in discuss-phase | — Decided (D-01) |
    ```

    No other changes to PROJECT.md.
  </action>
  <verify>
    <automated>grep -n "always produces an output file\|always produces output\|re-encode without cuts\|always produce output" /home/oton/projects/podcast-ad-cutter/.planning/PROJECT.md</automated>
  </verify>
  <done>PROJECT.md contains no instance of "always produces an output file", "always produces output", or "re-encode without cuts". The three stale statements now reflect the D-01 return-None behavior. The last grep must return no results.</done>
</task>

</tasks>

<verification>
All three planning documents consistently describe the D-01 behavior: AudioEditor returns None when no qualifying ads, pipeline preserves original URL.

Run:
```bash
grep -n "return None" /home/oton/projects/podcast-ad-cutter/.planning/REQUIREMENTS.md
grep -n "original episode URL" /home/oton/projects/podcast-ad-cutter/.planning/ROADMAP.md
grep -n "always produces an output file\|always produces output\|re-encode without cuts" \
  /home/oton/projects/podcast-ad-cutter/.planning/REQUIREMENTS.md \
  /home/oton/projects/podcast-ad-cutter/.planning/ROADMAP.md \
  /home/oton/projects/podcast-ad-cutter/.planning/PROJECT.md
```

The last grep must return no results.
</verification>

<success_criteria>
- REQUIREMENTS.md EDIT-02 contains "return None" and "original episode URL unchanged"
- ROADMAP.md Phase 2 success criterion 2 describes the return-None behavior
- PROJECT.md Active list, Context paragraph, and Key Decisions table no longer contain any always-produces-output language
- Neither file retains the phrase "always produces an output file", "always produces output", or "re-encode without cuts" in the relevant contexts
</success_criteria>

<output>
After completion, create `.planning/phases/02-pipeline-integration-ad-detection-audio-editing/02-01-SUMMARY.md`
</output>
