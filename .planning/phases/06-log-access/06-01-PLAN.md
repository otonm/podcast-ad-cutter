---
phase: 06-log-access
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/routes/logs.py
  - api/server.py
  - tests/test_api_control.py
  - tests/test_api_events.py
  - tests/test_api_feeds.py
  - tests/test_api_health.py
  - tests/test_api_db.py
  - tests/test_api_settings.py
autonomous: true
requirements: [LOG-01, LOG-02, LOG-03]

must_haves:
  truths:
    - "create_app() accepts log_dir as a required parameter and registers the logs router"
    - "All existing test files call create_app() with log_dir and the full suite stays green"
    - "create_logs_router(log_dir) exists and returns a web.RouteTableDef wired into create_app"
  artifacts:
    - path: "api/routes/logs.py"
      provides: "create_logs_router(log_dir) factory skeleton returning a RouteTableDef"
      contains: "def create_logs_router(log_dir: Path) -> web.RouteTableDef"
    - path: "api/server.py"
      provides: "create_app() with log_dir parameter and create_logs_router wiring"
      contains: "create_logs_router(log_dir)"
  key_links:
    - from: "api/server.py:serve"
      to: "create_app"
      via: "config.app.paths.log_dir passed as log_dir argument"
      pattern: "config\\.app\\.paths\\.log_dir"
    - from: "api/server.py:create_app"
      to: "create_logs_router"
      via: "app.add_routes(create_logs_router(log_dir))"
      pattern: "create_logs_router\\(log_dir\\)"
---

<objective>
Add the `log_dir: Path` dependency-injection seam (D-14) by changing the `create_app()` signature, wiring `config.app.paths.log_dir` through `serve()`, registering a (skeleton) `create_logs_router(log_dir)`, and updating ALL existing `create_app()` call sites in the test suite so the full suite stays green.

Purpose: This is the disruptive migration step flagged in CONTEXT.md — adding a required parameter to `create_app()` breaks ~40 call sites across 6 test files. Isolating it into a dedicated low-risk plan keeps the handler-implementation plans (02, 03) focused. It also establishes the `create_logs_router` contract before its handlers are implemented (interface-first ordering).
Output: `api/routes/logs.py` skeleton, modified `api/server.py`, and updated test call sites. Full test suite green at 100% coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-log-access/06-CONTEXT.md
@.planning/phases/06-log-access/06-RESEARCH.md
@.planning/phases/06-log-access/06-PATTERNS.md

<interfaces>
<!-- Current create_app signature (api/server.py lines 30-66) the executor extends -->
Current signature (api/server.py):
  def create_app(event_bus, start_time, run_state, config, config_path) -> web.Application
  serve() calls: create_app(event_bus, start_time, run_state, config, config_path)
  serve() already receives `config`; config.app.paths.log_dir is a Path (config/config_loader.py:57, PathsConfig.log_dir).

Existing factory pattern to mirror (api/routes/db.py:74):
  def create_db_router(db_path: Path, output_dir: Path, config_path: Path) -> web.RouteTableDef

create_app() call-site shape used across the test suite (all positional, 5 args):
  create_app(EventBus(), time.monotonic(), RunState(), <cfg>, <config_path>)

test_api_db.py uses a MagicMock cfg with cfg.app.paths.data_dir / cfg.app.paths.output_dir set.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add log_dir parameter to create_app and register skeleton logs router</name>
  <files>api/routes/logs.py, api/server.py</files>
  <read_first>
    - api/server.py (current create_app + serve signatures and route registration block, lines 30-98)
    - api/routes/db.py (factory shape to mirror: create_db_router lines 74-93)
    - config/config_loader.py (PathsConfig.log_dir at line 57 — confirm it is a Path)
  </read_first>
  <action>
    Create `api/routes/logs.py` with a module docstring and a factory `def create_logs_router(log_dir: Path) -> web.RouteTableDef:` that builds `routes = web.RouteTableDef()` and returns it with NO handlers yet (handlers come in plans 02 and 03). Use `from __future__ import annotations`, import `logging`, `Path` from `pathlib`, and `web` from `aiohttp`; add a module `logger = logging.getLogger(__name__)`. Mirror the docstring style of `create_db_router` in api/routes/db.py.

    In `api/server.py`: add `from api.routes.logs import create_logs_router` to the imports. Add `log_dir: Path` as the LAST positional parameter of `create_app()` (after `config_path`), making it required per D-14 (matches the required-arg pattern of create_db_router). Document the new param in the docstring. After the existing `app.add_routes(create_db_router(...))` block, add `app.add_routes(create_logs_router(log_dir))`. In `serve()`, update the `create_app(...)` call to pass `config.app.paths.log_dir` as the final argument — `serve()` already has `config`, so no main.py change is needed.

    Do NOT add any handlers, route decorators, or path-traversal logic in this task — that is plans 02 and 03. `Path` must be imported at runtime in logs.py (used in the signature), not under TYPE_CHECKING.
  </action>
  <verify>
    <automated>uv run python -c "from api.routes.logs import create_logs_router; from pathlib import Path; r = create_logs_router(Path('logs')); assert hasattr(r, '_items') or True; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - api/routes/logs.py contains `def create_logs_router(log_dir: Path) -> web.RouteTableDef`
    - api/server.py create_app() signature includes `log_dir: Path` as the final positional parameter
    - api/server.py imports `from api.routes.logs import create_logs_router`
    - api/server.py create_app body contains `app.add_routes(create_logs_router(log_dir))`
    - api/server.py serve() calls create_app(...) passing `config.app.paths.log_dir` as the final argument
    - `grep -c "config.app.paths.log_dir" api/server.py` returns at least 1
    - logs.py imports Path at runtime (not under TYPE_CHECKING) — `uv run python -c "from api.routes.logs import create_logs_router"` exits 0
  </acceptance_criteria>
  <done>create_app() requires log_dir, registers the skeleton logs router, and serve() supplies config.app.paths.log_dir; logs.py importable.</done>
</task>

<task type="auto">
  <name>Task 2: Update all existing create_app() call sites to pass log_dir</name>
  <files>tests/test_api_control.py, tests/test_api_events.py, tests/test_api_feeds.py, tests/test_api_health.py, tests/test_api_db.py, tests/test_api_settings.py</files>
  <read_first>
    - tests/test_api_db.py (the _make_app helper at lines 65-73 — needs cfg.app.paths.log_dir + log_dir arg)
    - tests/test_api_health.py (call sites at lines 28-56)
    - tests/test_api_events.py (call sites at lines 26-87)
    - tests/test_api_control.py (call sites — both _make_config and cfg variants)
    - tests/test_api_feeds.py (call sites at lines 96, 146, 382)
    - tests/test_api_settings.py (call sites at lines 33-215)
  </read_first>
  <action>
    Update every `create_app(...)` call across the six test files to pass a 6th positional argument: a `log_dir` path. Use `tmp_path / "logs"` as the log_dir value in each call (the fixture `tmp_path` is already in scope at every call site since they all reference `tmp_path / "config.yaml"` or `config_path` derived from tmp_path). The logs directory does NOT need to exist on disk for these non-log tests — the skeleton router has no handlers that read it, so passing the path is sufficient.

    For test_api_db.py and test_api_feeds.py which use MagicMock configs, no change to the MagicMock is required for these tests since `create_logs_router` (skeleton) does not read log_dir. Only append the `tmp_path / "logs"` argument to each create_app() call.

    For test_api_settings.py call sites that build `config_path` from a tmp dir, use the same tmp base: pass `config_path.parent / "logs"` if `tmp_path` is not directly in scope, otherwise `tmp_path / "logs"`. Match whichever tmp variable is already used at that call site.

    Do not change test assertions, fixtures, or any other logic — only extend the create_app() argument list.
  </action>
  <verify>
    <automated>uv run pytest tests/test_api_health.py tests/test_api_events.py tests/test_api_control.py tests/test_api_feeds.py tests/test_api_db.py tests/test_api_settings.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "create_app(" tests/ | grep -v "log" | grep -c "config.yaml\")$"` returns 0 (no 5-arg call site ending without log_dir remains)
    - `uv run pytest tests/test_api_health.py tests/test_api_events.py tests/test_api_control.py tests/test_api_feeds.py tests/test_api_db.py tests/test_api_settings.py -x` exits 0
    - No `TypeError: create_app() missing 1 required positional argument: 'log_dir'` in any test output
    - tests/test_api_server.py still passes unchanged (it calls serve(), not create_app()) — `uv run pytest tests/test_api_server.py -x` exits 0
  </acceptance_criteria>
  <done>All create_app() call sites pass log_dir; the existing test suite is green with the new required parameter.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| serve()→create_app | log_dir flows from config (trusted, operator-controlled) into the app factory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | create_app signature change breaking existing call sites | mitigate | Task 2 updates every call site; full suite must be green before plan completes (V5 input-validation surface unchanged in this plan) |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase — RESEARCH.md Package Legitimacy Audit confirms zero installs; nothing to verify |
</threat_model>

<verification>
- `uv run pytest --cov=.` — full suite green at 100% coverage (skeleton router adds no uncovered lines; the factory is exercised by every create_app() test).
- `uv run ruff` — no lint errors.
- `grep -c "create_logs_router(log_dir)" api/server.py` returns 1.
</verification>

<success_criteria>
- create_app() requires `log_dir: Path` and registers `create_logs_router(log_dir)`.
- serve() supplies `config.app.paths.log_dir`.
- api/routes/logs.py exists with the factory skeleton (no handlers).
- Full test suite green at 100% coverage; ruff clean.
</success_criteria>

<output>
Create `.planning/phases/06-log-access/06-01-SUMMARY.md` when done.
</output>
