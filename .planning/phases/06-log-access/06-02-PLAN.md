---
phase: 06-log-access
plan: 02
type: tdd
wave: 2
depends_on: ["06-01"]
files_modified:
  - api/routes/logs.py
  - tests/test_api_logs.py
autonomous: true
requirements: [LOG-01, LOG-02]

must_haves:
  truths:
    - "GET /api/v1/logs returns hierarchical JSON with app_logs and episode_logs keys"
    - "GET /api/v1/logs/{filename} returns the full file content as text/plain"
    - "?offset=N&limit=N returns the correct byte slice with X-Log-* headers"
    - "Path traversal (../) returns 400 and does not expose files outside log_dir"
    - "Requesting a missing file returns 404"
  artifacts:
    - path: "api/routes/logs.py"
      provides: "list_logs and read_log handlers plus _validate_path traversal guard"
      contains: "is_relative_to"
    - path: "tests/test_api_logs.py"
      provides: "TestLogList, TestLogRead, TestLogSecurity test classes"
      contains: "class TestLogSecurity"
  key_links:
    - from: "read_log handler"
      to: "_validate_path"
      via: "Path.resolve().is_relative_to(log_dir.resolve())"
      pattern: "is_relative_to"
    - from: "list_logs handler"
      to: "log_dir"
      via: "asyncio.to_thread directory scan of *.log and episodes/<slug>/*.log"
      pattern: "asyncio\\.to_thread"
---

<objective>
Implement the two request/response log endpoints in `api/routes/logs.py` using TDD: `GET /api/v1/logs` (LOG-01, hierarchical listing) and `GET /api/v1/logs/{tail:.*}` (LOG-02, full/paginated content with byte-range headers), plus the shared `_validate_path` path-traversal guard (D-05) returning 400. Write failing tests first in `tests/test_api_logs.py`, then implement.

Purpose: Deliver the listing and download capability of Phase 6. The SSE tail endpoint (LOG-03) is plan 03 — but the `{tail:.*}/tail` route MUST already be registered BEFORE the `{tail:.*}` glob (verified critical ordering bug). To avoid a same-file conflict, this plan registers the `/tail` route as a thin placeholder that returns 501 and is fully implemented in plan 03; the glob `read_log` route is registered AFTER it.
Output: list_logs + read_log handlers, _validate_path helper, a placeholder tail route preserving registration order, and TestLogList/TestLogRead/TestLogSecurity test classes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-log-access/06-CONTEXT.md
@.planning/phases/06-log-access/06-RESEARCH.md
@.planning/phases/06-log-access/06-PATTERNS.md
@api/routes/db.py
@api/routes/logs.py

<interfaces>
<!-- Decisions that constrain the implementation -->
D-01: list response shape = {"app_logs": [...], "episode_logs": {"<feed-slug>": [...]}}
      app_logs = top-level logs/*.log; episode_logs = logs/episodes/<feed-slug>/*.log grouped by slug.
D-02: each entry = {"filename": <relative path from log_dir>, "size_bytes": int, "last_modified": ISO 8601 UTC}
D-04: routes = "/api/v1/logs/{tail:.*}/tail" then "/api/v1/logs/{tail:.*}" (tail FIRST).
D-05: traversal guard = (log_dir / tail).resolve().is_relative_to(log_dir.resolve()); 400 (HTTPBadRequest) if not.
D-06: content response Content-Type text/plain; charset=utf-8; raw body, no JSON wrapping.
D-07: pagination via headers X-Log-Size (total bytes), X-Log-Offset (start), X-Log-Limit (bytes returned);
      ?offset / ?limit are byte offsets; no offset = full file; no limit = to EOF.
A3 (RESEARCH): if log_dir does not exist, listing returns {"app_logs": [], "episode_logs": {}} (not 404).

Query-param ValueError pattern (from db.py): catch ValueError on int() parse, raise web.HTTPBadRequest(...) from None.
All file I/O (glob, stat, read_bytes, exists) MUST be wrapped in asyncio.to_thread (CLAUDE.md async rule).
Test app helper: build via create_app(EventBus(), time.monotonic(), RunState(), MagicMock cfg, config_path, log_dir)
  with cfg.app.paths.log_dir = tmp_path / "logs"; create the log_dir on disk for tests that read files.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: TDD list endpoint and traversal guard (LOG-01 + D-05)</name>
  <files>api/routes/logs.py, tests/test_api_logs.py</files>
  <read_first>
    - api/routes/logs.py (the skeleton create_logs_router from plan 01)
    - api/routes/db.py (json_response usage line 147; query-param ValueError handling lines 97-108)
    - tests/test_api_db.py (TestClient/TestServer usage and _make_app helper lines 60-79)
    - .planning/phases/06-log-access/06-RESEARCH.md (Pattern 2 traversal, Pattern 4 listing, LOG-01 code example lines 353-384)
  </read_first>
  <behavior>
    - GET /api/v1/logs returns 200 and {"app_logs": [], "episode_logs": {}} when log_dir does not exist
    - GET /api/v1/logs lists top-level *.log files under app_logs, each with filename (relative), size_bytes (int), last_modified (ISO 8601)
    - episode logs under logs/episodes/<slug>/*.log appear under episode_logs grouped by slug; filename is the path relative to log_dir (e.g. "episodes/prof-g/ep.log")
    - _validate_path returns the resolved path for a valid relative name and raises HTTPBadRequest (400) for "../etc/passwd" style traversal
  </behavior>
  <action>
    Add a test helper `_make_app(tmp_path)` to tests/test_api_logs.py mirroring tests/test_api_db.py: MagicMock cfg with cfg.app.paths.data_dir = tmp_path, cfg.app.paths.output_dir = tmp_path/"output", cfg.app.paths.log_dir = tmp_path/"logs"; write a minimal config_path; call create_app(EventBus(), time.monotonic(), RunState(), cfg, config_path, tmp_path/"logs"). Write TestLogList (empty, app logs present, episode logs grouped) and a TestLogSecurity test asserting GET /api/v1/logs/../etc/passwd returns 400.

    Implement in api/routes/logs.py: a module-level `_validate_path(log_dir: Path, tail: str) -> Path` that does `target = (log_dir / tail).resolve()` and `if not target.is_relative_to(log_dir.resolve()): raise web.HTTPBadRequest(text="Invalid path")`, returning target. Add a synchronous `_list_logs_sync(log_dir)` that builds the D-01 structure: app_logs from `sorted(log_dir.glob("*.log"), key=...)`, episode_logs by iterating sorted `log_dir/"episodes"` subdirectories; guard with `if not log_dir.exists(): return {"app_logs": [], "episode_logs": {}}`. Each entry uses `str(f.relative_to(log_dir))`, `stat.st_size`, and `datetime.fromtimestamp(stat.st_mtime, tz=timezone.utc).isoformat()`. Register `@routes.get("/api/v1/logs")` `list_logs` that does `result = await asyncio.to_thread(_list_logs_sync, log_dir)` and returns `web.json_response(result)`. Import `asyncio` and `datetime, timezone`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_api_logs.py::TestLogList tests/test_api_logs.py::TestLogSecurity -x</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_api_logs.py contains `class TestLogList` and `class TestLogSecurity`
    - api/routes/logs.py contains `def _validate_path` using `is_relative_to`
    - GET /api/v1/logs returns {"app_logs": [], "episode_logs": {}} when log_dir absent (test asserts exact dict)
    - GET /api/v1/logs groups episode logs by feed slug under episode_logs (test asserts slug key present)
    - GET /api/v1/logs/../etc/passwd returns status 400
    - `uv run pytest tests/test_api_logs.py::TestLogList tests/test_api_logs.py::TestLogSecurity -x` exits 0
    - all file I/O in list_logs goes through asyncio.to_thread — `grep -c "asyncio.to_thread" api/routes/logs.py` >= 1
  </acceptance_criteria>
  <done>List endpoint returns the D-01 hierarchical shape and _validate_path rejects traversal with 400; tests green.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: TDD read/paginate endpoint with byte-range headers (LOG-02 + D-06/D-07)</name>
  <files>api/routes/logs.py, tests/test_api_logs.py</files>
  <read_first>
    - api/routes/logs.py (now has list_logs + _validate_path from task 1)
    - .planning/phases/06-log-access/06-RESEARCH.md (LOG-02 code example lines 386-423; route registration order lines 425-437)
    - api/routes/db.py (query-param ValueError handling lines 97-108)
  </read_first>
  <behavior>
    - GET /api/v1/logs/{filename} returns 200 with the full file content and Content-Type "text/plain; charset=utf-8"
    - X-Log-Size equals total file bytes; X-Log-Offset = 0 and X-Log-Limit = total when no offset/limit given
    - ?offset=N&limit=M returns bytes [N:N+M]; X-Log-Offset = N, X-Log-Limit = len(returned)
    - non-integer offset or limit returns 400
    - missing file returns 404
    - path traversal on the read route returns 400
  </behavior>
  <action>
    In api/routes/logs.py, register the `/tail` route placeholder FIRST to lock in correct ordering (D-04): `@routes.get("/api/v1/logs/{tail:.*}/tail")` `tail_log` that for now raises `web.HTTPNotImplemented()` (full SSE implementation is plan 03). Immediately AFTER it register `@routes.get("/api/v1/logs/{tail:.*}")` `read_log`.

    read_log: `tail = request.match_info["tail"]`; `log_path = _validate_path(log_dir, tail)` (raises 400 on traversal); if not `await asyncio.to_thread(log_path.exists)` raise `web.HTTPNotFound()`. Parse offset/limit from request.rel_url.query as optional ints (None when absent) using the db.py ValueError pattern, raising HTTPBadRequest with text "offset and limit must be integers" on bad input. Define a sync `read_slice(path)` that reads `path.read_bytes()`, computes `total = len(data)`, `start = offset or 0`, `chunk = data[start:start+limit] if limit is not None else data[start:]`, returning `(chunk, total, start, len(chunk))`; call it via `await asyncio.to_thread(read_slice, log_path)`. Return `web.Response(body=chunk, content_type="text/plain", charset="utf-8", headers={"X-Log-Size": str(total), "X-Log-Offset": str(start), "X-Log-Limit": str(returned)})`.

    Write TestLogRead tests in tests/test_api_logs.py: full content + headers, byte-range slice correctness, non-integer offset/limit -> 400, missing file -> 404. Add a TestLogSecurity test for traversal on the read route (`/api/v1/logs/../../etc/passwd` -> 400). Confirm the tail placeholder does not shadow read_log (a request to /api/v1/logs/foo.log returns the file, not 501).
  </action>
  <verify>
    <automated>uv run pytest tests/test_api_logs.py::TestLogRead tests/test_api_logs.py::TestLogSecurity -x</automated>
  </verify>
  <acceptance_criteria>
    - api/routes/logs.py registers "/api/v1/logs/{tail:.*}/tail" BEFORE "/api/v1/logs/{tail:.*}" (tail route line number < read route line number)
    - GET /api/v1/logs/<file> returns Content-Type containing "text/plain" and "charset=utf-8"
    - response sets X-Log-Size, X-Log-Offset, X-Log-Limit headers (test asserts all three)
    - ?offset=3&limit=4 returns exactly bytes [3:7] of the file (test asserts byte content)
    - GET /api/v1/logs/<file>?offset=abc returns 400
    - GET /api/v1/logs/does-not-exist.log returns 404
    - GET /api/v1/logs/../../etc/passwd returns 400
    - GET /api/v1/logs/<existing file> returns the file body, not 501 (read route not shadowed by tail placeholder)
    - `uv run pytest tests/test_api_logs.py::TestLogRead tests/test_api_logs.py::TestLogSecurity -x` exits 0
  </acceptance_criteria>
  <done>Read endpoint serves full and byte-sliced content with correct headers and status codes; route order locked; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→read_log | Untrusted `{tail}` path segment crosses into filesystem access |
| client→read_log query | Untrusted `?offset`/`?limit` integers control byte-slice arithmetic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Information Disclosure | read_log path handling | mitigate | _validate_path resolves and checks is_relative_to(log_dir.resolve()); traversal returns 400 (D-05); tested in TestLogSecurity |
| T-06-03 | Tampering | offset/limit integer parsing | mitigate | int() parse wrapped in try/except ValueError -> HTTPBadRequest 400; negative/oversized offsets produce empty slice via Python slicing (no error, no escape) |
| T-06-04 | Denial of Service | read_bytes loads whole file (A1) | accept | Log files are bounded by rotation config (keep_last); local trusted-operator tool; no explicit size cap required by LOG-02 |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No new packages — RESEARCH.md Package Legitimacy Audit confirms zero installs |
</threat_model>

<verification>
- `uv run pytest tests/test_api_logs.py tests/test_api_server.py -x` — green after each task.
- `uv run pytest --cov=.` — 100% coverage including logs.py list_logs + read_log (the tail placeholder line is exercised by a 501 assertion or marked appropriately; full tail coverage lands in plan 03).
- `uv run ruff` — clean.
- Route order check: tail route declared before glob route in logs.py.
</verification>

<success_criteria>
- GET /api/v1/logs returns the D-01 hierarchical JSON (empty when no log_dir).
- GET /api/v1/logs/{filename} returns full and byte-sliced content (D-06/D-07) with X-Log-* headers.
- Path traversal returns 400; missing file returns 404; bad offset/limit returns 400.
- `{tail:.*}/tail` route registered before `{tail:.*}` glob.
- Tests green; ruff clean.
</success_criteria>

<output>
Create `.planning/phases/06-log-access/06-02-SUMMARY.md` when done.
</output>
